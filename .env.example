# Variables d'environnement pour le déploiement sur Render

# URL de la base de données PostgreSQL (fournie automatiquement par Render)
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# Clé secrète pour Flask (optionnel, une valeur par défaut est définie dans le code)
SECRET_KEY=your_secret_key_here

